#include <string.h>
#include "main.h"

/**
 * _strcmp - compares two strings
 * *@s1: first string to be compared
 * *@s2: second ring to be compared
 * Return: +/-/0 depending on the comparison.
 */
int _strcmp(char *s1, char *s2)
{
	int res;

	res = strcmp(s1, s2);
	return (res);
}
