#include <stdio.h>
#include "main.h"

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char *string = "_putchar";

	for (i = 0, string[i] = '\0', i++);
	{
		char c = *string[i];
		_putchar ('c');
	}
	putchar('\n');
	return (0);
}
