#include "main.h"

/**
 * main - function to print an infinite loop
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int i;
	
	printf("Infinite loop incoming :(\n");
	i = 0;
	/**
	 * infinit loop comment
	 * while (i< 10)
	 * {
	 * putchar(i);
	 * }
	 */

printf("infinite loop avoided! \\o/\n");
return (0);
}
