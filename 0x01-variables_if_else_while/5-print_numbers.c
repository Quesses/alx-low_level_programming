#include <stdio.h>
/**
 * main - Printsall the single digits of base 10 (i.e 1 through 9)
 *
 * Return: Always 0 on success.
 */
int main(void)
{
	int n;
	for (n = 0; n < 10; n++)
		printf("%d", n);
	printf("\n");
	return (0);
}
