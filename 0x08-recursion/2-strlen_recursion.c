#include "main.h"
#include <string.h>

/**
 * _strlen_recursion - prints the length of a string
 * @s: the given string which length is to be printed
 *
 * Return: length of the string
 */
int _strlen_recursion(char *s)
{
	int len;

	len = strlen(s);
	return (len);
}
