#include "main.h"

/**
 * is_palindrome - determines if a given string is a palindrome or not
 * @s: given string to be checked
 *
 * Return: 1 if it is a palindrome, 0 otherwise.
 */
int is_palindrome(char *s)
{
	int
