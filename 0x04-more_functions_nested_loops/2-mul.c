#include "main.h"
#include <stdio.h>

/**
 * mul - prints the product of a and b
 * @a: first integer
 * @b: second integer
 *
 * Return: returns a by b
 */

int mul(int a, int b)
{
	mul(a, b);
	return (a *b);
}
