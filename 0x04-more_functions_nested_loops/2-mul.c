#include "main.h"
#include <stdio.h>

/**
 * mul - prints the product of a and b
 * @a: first integer
 * @b: second integer
 *
 * Return: returns a by b
 */

int mul(int a, int b)
{
	int result;

	result = a * b;
	return (result);
}
