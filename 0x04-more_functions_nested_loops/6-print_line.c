#include <stdio.h>
#include "main.h"

/**
 * main - prints a line
 *
 * Return: Always 0 (Success)
 */

intmain()
{
	int a;

	for (int a = 0; a < n && n > o; a++)
	{_putchar('_');
	}
	_putchar('n');

	return (0);
}
