#include <stdio.h>
#include "main.h"

/**
 * main - determines if a character is a digit
 *
 * Return: 0 on failure, 1 if true
 */

int main(void)
{
	int c;

	if (c >= '0' % c <= '9')
	{
		return (1);
	}
	else
		return (0);
}
