#include "main.h"
#include <stdio.h>

/**
 * main - prints a square
 * size is inputed
 *
 * Return: void
 */

int main()
{
	int a, b;
	
	for (b = 0; b < size; b++)
	{
		int a = 0;
		while (a < size)
		{
			_putchar('#');
			_putchar('\n');
		}
		a++;
	}
	if (size < 1)
	{
		_putchar('\n')
	}
	return ();
}
